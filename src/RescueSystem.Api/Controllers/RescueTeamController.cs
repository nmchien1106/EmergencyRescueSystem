using Microsoft.AspNetCore.Http;
using Microsoft.AspNetCore.Mvc;

namespace RescueSystem.Api.Controllers
{
    [Route("api/rescue-teams")]
    [ApiController]
    public class RescueTeamController : ControllerBase
    {
        // GET: api/rescue-teams
        // GET: api/rescue-teams/{id}
        // POST: api/rescue-teams
        // PUT: api/rescue-teams/{id}
        // DELETE: api/rescue-teams/{id}
        // GET : api/rescue-teams/{id}/members
        // POST : api/rescue-teams/{id}/members
        // DELETE : api/rescue-teams/{id}/members/{memberId}
        // GET : api/rescue-teams/{id}/missions
        // POST : api/rescue-teams/{id}/missions
        // DELETE : api/rescue-teams/{id}/missions/{missionId}
    }
}
